% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_correlation.R
\name{corr_table}
\alias{corr_table}
\title{Formulate Formatted Correlation Table}
\usage{
corr_table(
  x = NULL,
  y = NULL,
  data = NULL,
  formula = NULL,
  method = c("pearson", "spearman", "kendall"),
  conf.level = 0.95,
  alternative = c("two.sided", "less", "greater"),
  title = NULL,
  plot_title = NULL,
  add_smooth = TRUE,
  point_alpha = 0.7,
  return_plot = FALSE,
  round_digits = 3,
  ...
)
}
\arguments{
\item{x, y}{Numeric vectors of data values. \code{x} and \code{y} must have the same length.}

\item{data}{Operational matrix or dataframe containing the variales in \code{formula}.}

\item{method}{Character string. Correlation coefficient to be calculated. Must be one of \code{"pearson"}, \code{"kendall"}, or \code{"spearman"}.}

\item{conf.level}{Numeric. Confidence level for the calculated confidence interval. Default 0.95.}

\item{alternative}{Character string. Alternative hypothesis. Must be one of \code{"two.sided"}, \code{"less"}, \code{"greater"}.}

\item{title}{Character string. Table title.}

\item{plot_title}{Character string. Plot title.}

\item{add_smooth}{Logical. Should a smoothed line of best fit be presented on the plot? Default is \code{TRUE}.}

\item{return_plot}{Logical. Should a plot be returned? Default is \code{FALSE}.}

\item{round_digits}{Numeric. Number of digits to round outputs to.}

\item{...}{Miscellaneous arguments that can be passed to \code{stats::cor.test}.}
}
\value{
A list of ggplot2 theme elements and scale adjustments.
}
\description{
This function calculates the correlation between two continuous variables and formats the outputs, utilising pre-existing functionalities of the package.
}
\note{
If a Google font has not been loaded with the package, \code{thekids_theme} will load this function on your behalf.

Supported Google Fonts are those that exist in \code{sysfonts::font_families_google()}.
}
\examples{
corr_table(mtcars$mpg,
           mtcars$hp)

}
