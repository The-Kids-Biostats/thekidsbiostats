% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_colnames.R
\name{update_columns}
\alias{update_columns}
\title{Apply a variable dictionary to a dataset. Each column is replaced with a new name and a corresponding label attribute is applied.}
\usage{
update_columns(
  data,
  dict,
  old = NULL,
  new = NULL,
  label = NULL,
  reorder = FALSE
)
}
\arguments{
\item{data}{A data.frame-like object}

\item{dict}{A data.frame-like object or .csv file path (.csv, .txt, .xlsx, .rds). Should have 3 columns specifying: \code{old} names, \code{new} names, and \code{label}s.}

\item{old}{Column name in dict with old column names (default "old")}

\item{new}{Column name in dict with new column names (default "new")}

\item{label}{Column name in dict with human-readable labels (default "label")}

\item{reorder}{Logical, default \code{FALSE}. If \code{TRUE}, columns listed in the dictionary will be
reordered according to the order they appear in the dictionary.
Columns not referenced in the dictionary remain at the end in their original order.}
}
\value{
A data.frame with renamed columns and labels applied
}
\description{
Apply a variable dictionary to a dataset. Each column is replaced with a new name and a corresponding label attribute is applied.
}
\examples{
data("data_patient", package = "thekidsbiostats")

# 1) Create a data dictionary and formulate some cleaned column names, assign to `dict` object
dict <- make_column_dict(data_patient, auto_clean = TRUE, quiet = FALSE)

# 2) Apply column names to data, assign to `data_patient_clean`
data_patient_clean <- update_columns(data_patient, dict = dict)

}
