# Insert Callout via RStudio Addin

Inserts a Quarto callout block with placeholder text directly at the
cursor.

## Usage

``` r
insert_callout_2()
```

## Value

None. Inserts text into the active RStudio document.

## Examples

``` r
if (FALSE) { # \dontrun{
insert_callout()
} # }
```
